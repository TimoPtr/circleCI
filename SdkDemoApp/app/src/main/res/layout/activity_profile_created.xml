<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools"
  android:layout_width="match_parent"
  android:layout_height="match_parent">

  <TextView
    android:id="@+id/title"
    style="@style/Title"
    android:text="@string/profile_created_title" />

  <Button
    android:id="@+id/profile_created_ok"
    style="@style/PrimaryColorRoundButton"
    android:layout_alignParentBottom="true"
    android:layout_centerHorizontal="true"
    android:layout_gravity="center"
    android:layout_marginBottom="20dp"
    android:text="@android:string/ok" />

  <ImageView
    android:id="@+id/tick"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_centerInParent="true"
    android:src="@drawable/ic_profile_created"
    tools:ignore="ContentDescription" />

  <TextView
    android:layout_width="match_parent"
    android:layout_height="0dp"
    android:layout_above="@id/profile_created_ok"
    android:layout_below="@id/tick"
    android:gravity="center"
    android:padding="20dp"
    android:text="@string/profile_created_message"
    android:textColor="@color/grey_light"
    android:textSize="16sp" />
</RelativeLayout>